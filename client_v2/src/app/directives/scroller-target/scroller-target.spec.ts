import { ScrollerTarget } from './scroller-target';

describe('ScrollerTarget', () => {
  it('should create an instance', () => {
    const directive = new ScrollerTarget();
    expect(directive).toBeTruthy();
  });
});
