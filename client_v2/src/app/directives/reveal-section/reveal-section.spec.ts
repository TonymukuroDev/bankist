import { RevealSectionDirective } from './reveal-section';

describe('DRevealSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new RevealSectionDirective();
    expect(directive).toBeTruthy();
  });
});
